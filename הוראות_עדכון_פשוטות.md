# 🎯 הוראות עדכון - צעד אחר צעד

## שלב 1: עדכון `daily-economic-sync-simple`

1. **לחץ על `daily-economic-sync-simple`** ברשימה (27 deployments)

2. **בדף שנפתח:**
   - לחץ על **"Edit"** או **"Code"** (כפתור עריכה)

3. **העתק את כל הקוד** מהקובץ:
   ```
   supabase/functions/daily-economic-sync-simple/index.ts
   ```
   
   (או העתק את הקוד שמופיע כאן למעלה)

4. **הדבק** במקום הקוד הישן

5. **שמור ופרוס:**
   - לחץ **"Deploy"** או **"Save & Deploy"**

---

## שלב 2: בדיקה

1. **בדף הפונקציה**, לחץ על **"Invoke"** או **"Test"**
2. **בדוק את הלוגים:**
   - צריך לראות: `📊 Fetched X events`
   - צריך לראות: `📅 Event: [שם] - Original: [תאריך] → Parsed: [תאריך מתוקן]`

---

## שלב 3: (אופציונלי) עדכון Cron Job

אם יש cron job שמריץ את הפונקציה, עדכן אותו:

1. לך ל-**SQL Editor** ב-Supabase
2. הרץ את הקובץ: `update_economic_calendar_cron.sql`

או הרץ את הפקודה:

```sql
SELECT cron.schedule(
  'daily-economic-sync-simple',
  '0 6 * * *',
  $$
  SELECT net.http_post(
    url := 'https://wpmrtczbfcijoocguime.supabase.co/functions/v1/daily-economic-sync-simple',
    headers := '{"Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndwbXJ0Y3piZmNpam9vY2d1aW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDczNTEsImV4cCI6MjA2Njc4MzM1MX0.YHfniy3w94LVODC54xb7Us-Daw_pRx2WWFOoR-59kGQ", "Content-Type": "application/json"}'::jsonb,
    body := '{}'::jsonb
  );
  $$
);
```

---

## ✨ זה הכל!

אחרי הפריסה, האירועים הכלכליים יופיעו עם:
- ✅ תאריכים נכונים
- ✅ שעות נכונות (שעון ישראל)
- ✅ פריסה נכונה לימים


