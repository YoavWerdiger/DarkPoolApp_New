# בדיקת התראה למכשיר שלך (user_id אחד)

המכשיר רשום עם הטוקן – אם ההתראה עדיין לא מגיעה, רוב הסיכויים שזה אחד מהדברים הבאים.

---

## 1. לשלוח התראה בדיקה ידנית

ב־**Supabase Dashboard**:

1. **Edge Functions** → **send-push-notification** → **Invoke**
2. Body (JSON):

```json
{
  "userIds": ["c9b274a6-01cc-4b8e-89dd-f3834238f3b1"],
  "title": "בדיקה",
  "body": "אם אתה רואה את זה – ההתראות עובדות"
}
```

3. Invoke ולבדוק את **התשובה**:

- **Status 500 + "EXPO_ACCESS_TOKEN not configured"**  
  → צריך להגדיר את הסוד `EXPO_ACCESS_TOKEN` ב־Edge Functions → Secrets (ולרענן/לעשות deploy אם צריך).

- **Status 200, `sent: 0`, ו־`failed_details` עם הודעות**  
  → להסתכל על `failed_details`:  
  - `InvalidCredentials` / `Credentials are invalid` – בדרך כלל חסר או שגוי EXPO_ACCESS_TOKEN.  
  - `DeviceNotRegistered` – הטוקן לא תקף (אפליקציה הוסרה, טוקן השתנה). לנסות להיכנס שוב לאפליקציה כדי לרשום טוקן מחדש.  
  - `MessageTooBig` – גוף ההתראה גדול מדי.

- **Status 200, `sent: 1`**  
  → Expo קיבל את ההתראה. אם לא רואים על המכשיר:  
  - וידוא שהמכשיר לא ב־"לא להפריע" / Focus.  
  - בדיקת הגדרות האפליקציה: התראות מופעלות.  
  - אנדרואיד: הגדרות → אפליקציות → DarkPool → התראות – וידוא שהערוץ לא מושתק.

---

## 2. EXPO_ACCESS_TOKEN (חובה בפרודקשן)

בלי זה Expo לא ישלח לבילד production.

1. [expo.dev](https://expo.dev) → התחברות → **Account** → **Access Tokens**
2. ליצור Token (או להעתיק קיים)
3. Supabase → **Project Settings** → **Edge Functions** → **Secrets**  
   או: **Edge Functions** → לבחור פונקציה → **Secrets**
4. להוסיף:  
   **Name:** `EXPO_ACCESS_TOKEN`  
   **Value:** הערך של הטוקן מ־Expo

אם הוספת/עדכנת סוד – אין חובה לעשות deploy מחדש לפונקציה; הסוד נטען בהרצה.

---

## 3. לוגים של הפונקציה

אחרי ה־Invoke:

- **Edge Functions** → **send-push-notification** → **Logs**

בלוגים תופיע:

- האם יש שימוש ב־Expo Access Token
- אם Expo החזיר כישלונות – השורות עם `❌ Expo Push API returned errors` והפירוט של כל `status` / `details`

זה נותן את הסיבה המדויקת (טוקן לא תקף, credentials וכו').

---

## 4. אנדרואיד – FCM

אם אתה על **בילד production (APK/AAB)** ואתה רואה `sent: 1` אבל שום דבר לא מגיע:

- ב־`app.json` כבר מופיע `googleServicesFile: "./google-services.json"` – טוב.
- וודא ש־`google-services.json` קיים בשורש הפרויקט (אותו נתיב כמו ב־app.json) ושהבילד נבנה עם הקובץ (למשל ב־EAS Build).
- אם החלפת פרויקט Firebase או קובץ – לבנות מחדש את האפליקציה ולוודא שהמשתמש מקבל טוקן חדש אחרי התקנה.

---

## סיכום

1. Invoke ל־**send-push-notification** עם ה־userIds למעלה.
2. אם יש 500 / EXPO_ACCESS_TOKEN – להוסיף את הסוד.
3. אם יש `failed_details` – לקרוא את השגיאה בלוגים/בתשובה.
4. אם `sent: 1` אבל אין התראה – לבדוק השתקה/הגדרות במכשיר ו־FCM/בילד.

אם תשלח את התשובה מה־Invoke (בלי לחשוף מפתחות) או צילום מסך מה־Logs אחרי הבדיקה, אפשר לדייק עוד.
