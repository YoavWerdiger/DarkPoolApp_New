# הוראות העלאת עיצוב לקארדקום (LPC5)

## קבצים
- `cardcom_darkpool_custom.html` — HTML מותאם (class על כפתור + תקנון)
- `cardcom_darkpool_custom_style.css` — CSS מותאם

**חובה להדביק גם HTML וגם CSS** בעורך Studio. CSS לבד לא מספיק אם CardCom צובע את הכפתור בירוק.

### מה שונה ב-HTML (מינימלי)
- כפתור תשלום: `class="dp-pay-btn"` (נשארים `type`, `role`, וכל ה-`data-bind`)
- קישור תקנון: `class="dp-terms-link"`
- עטיפת כפתור: `class="submitDiv dp-submit"` (למרכוז)

---

## מה לעשות עכשיו ב-Studio

1. היכנס ל: **הגדרות → סליקה בפרופיל נמוך → עיצוב מותאם אישית CSS ו-HTML גרסה 5**
2. בחר **עיצוב בעבודה** (לא "מותאם אישית שאושר")
3. פתח **דף תצוגה מקדימה** בלשונית נפרדת (אל תסגור)
4. בעורך **HTML**: מחק הכל → הדבק את **כל** תוכן `cardcom_darkpool_custom.html` → שמור
5. בעורך **CSS**: מחק הכל → הדבק את **כל** תוכן `cardcom_darkpool_custom_style.css` → שמור
6. רענן את התצוגה המקדימה (F5)
7. אם נראה טוב → **הגש עיצוב לאישור**
8. אחרי אישור: ודא שהטרמינל משתמש ב-**מותאם אישית שאושר** (ולא בעיצוב ברירת מחדל)

---

## מה אמור להיראות

- רקע כהה
- כרטיסי טופס אפורים עם מסגרת
- כפתור תשלום אפור/זכוכית `#2a2a2a` עם טקסט לבן — **בלי ירוק**
- קישור תקנון באפור בהיר `#CCCCCC` / לבן ב-hover
- Footer עם לוגו/טקסט CardCom נשאר גלוי
- שדות טלפון/אימייל של בעל הכרטיס **לא** מוסתרים (3DS)

---

## אם כמעט אין שינוי

1. ודא שהדבקת **גם HTML בעבודה וגם CSS בעבודה**
2. ודא שב-HTML יש `class="dp-pay-btn"` על כפתור ה-Go / לתשלום
3. ודא ששמרת בשני העורכים
4. רענן את **תצוגה מקדימה** (לא את עמוד ההגדרות)
5. אל תסתכל על עיצוב "שאושר" ישן — רק על עיצוב בעבודה עד לאישור

---

## אל תיגע ב-

- מאפייני `data-bind` ב-HTML
- הסתרת שדות קלט חובה דרך CSS
- משפט Footer על סליקה דרך CardCom
