# 🧪 הוראות בדיקת התראה Push - צעד אחר צעד

## 📱 בדיקה מלאה של מערכת התראות

### שלב 1: וידוא שיש לך Device Token

**באפליקציה:**
1. פתח את האפליקציה
2. התחבר למשתמש שלך
3. האפליקציה תרשום את ה-device token אוטומטית (אחרי 2 שניות)

**במסד הנתונים - בדוק שהתרשם:**
```sql
-- בדוק אם יש לך device token פעיל
SELECT 
  id,
  user_id,
  expo_push_token,
  device_id,
  platform,
  is_active,
  created_at
FROM device_tokens
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'YOUR_EMAIL@example.com')
  AND is_active = true;
```

**אם אין device token:**
- פתח את האפליקציה שוב והתחבר
- חכה כמה שניות
- בדוק שוב

---

### שלב 2: בדיקת הגדרות התראות שלך

**באפליקציה:**
1. לך למסך **פרופיל** > **התראות**
2. ודא ש"**התראות חדשות**" **מופעלות** ✅
3. ודא ש"**התראות כלליות**" **מופעלות** ✅

**במסד הנתונים - בדוק שההגדרות נשמרו:**
```sql
-- בדוק את ההגדרות שלך
SELECT 
  user_id,
  notifications_enabled,
  news_notifications,
  sound_enabled
FROM user_notification_settings
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'YOUR_EMAIL@example.com');
```

**אם אין הגדרות:**
```sql
-- צור הגדרות ברירת מחדל
INSERT INTO user_notification_settings (user_id)
VALUES ((SELECT id FROM auth.users WHERE email = 'YOUR_EMAIL@example.com'))
ON CONFLICT (user_id) DO NOTHING;
```

---

### שלב 3: יצירת חדשה חדשה לבדיקה

**במסד הנתונים - הוסף חדשה חדשה:**

```sql
-- הוסף חדשה חדשה לבדיקה
INSERT INTO app_news (
  title,
  content,
  source,
  label,
  text_content,
  summary,
  source_url,
  image_url
)
VALUES (
  '🧪 בדיקת התראות Push - ' || TO_CHAR(NOW(), 'HH24:MI:SS'),
  'זו חדשה חדשה לבדיקת מערכת התראות Push. אם אתה רואה את זה, המשמעות היא שהמערכת עובדת!',
  'מערכת בדיקה',
  'בדיקת התראות',
  'זו חדשה חדשה לבדיקת מערכת התראות Push',
  'בדיקה',
  NULL,
  NULL
)
RETURNING id, title, created_at;
```

**שמור את ה-`id` שהתקבל** - נצטרך אותו לבדיקה!

---

### שלב 4: בדיקה שההתראה נוצרה

```sql
-- בדוק שההתראות נוצרו ב-pending_notifications
SELECT 
  pn.id,
  pn.user_id,
  pn.title,
  pn.body,
  pn.notification_type,
  pn.is_sent,
  pn.created_at,
  u.email as user_email,
  uns.news_notifications as user_wants_news
FROM pending_notifications pn
LEFT JOIN auth.users u ON pn.user_id = u.id
LEFT JOIN user_notification_settings uns ON pn.user_id = uns.user_id
WHERE pn.notification_type = 'news'
  AND pn.created_at > NOW() - INTERVAL '5 minutes'
ORDER BY pn.created_at DESC;
```

**מה לחפש:**
- ✅ יש התראות ב-`pending_notifications`
- ✅ רק למשתמשים עם `news_notifications = true`
- ✅ `is_sent = false` (עדיין לא נשלחו)

---

### שלב 5: בדיקה שההתראה נשלחה

**המתן כ-5-10 שניות** אחרי הוספת החדשה החדשה.

```sql
-- בדוק שההתראות נשלחו
SELECT 
  pn.id,
  pn.user_id,
  pn.title,
  pn.is_sent,
  pn.created_at,
  pn.sent_at,
  u.email as user_email
FROM pending_notifications pn
LEFT JOIN auth.users u ON pn.user_id = u.id
WHERE pn.notification_type = 'news'
  AND pn.created_at > NOW() - INTERVAL '5 minutes'
ORDER BY pn.created_at DESC;
```

**מה לחפש:**
- ✅ `is_sent = true` - ההתראות נשלחו!
- ✅ `sent_at` - מתי נשלחו

---

### שלב 6: בדיקה בפועל באפליקציה

**באפליקציה:**

1. **סגור את האפליקציה לחלוטין** (לא רק ברקע - סגור אותה)
   - iOS: החלק למעלה ושחרר
   - Android: החלק למצב האחרונות ומחק את האפליקציה

2. **המתן 10-30 שניות**

3. **אמור לקבל התראה Push** 📱
   - כותרת: "חדשה חדשה! 📰"
   - תוכן: טקסט החדשה החדשה שהוספת

4. **לחץ על ההתראה**
   - האפליקציה אמורה להיפתח
   - אמור לעבור למסך חדשות

---

### שלב 7: בדיקת Logs של Edge Function

**ב-Supabase Dashboard:**

1. היכנס ל-Supabase Dashboard
2. עבור ל-Edge Functions > `process-pending-notifications` > Logs
3. חפש את הקריאה האחרונה (אחרי הוספת החדשה החדשה)

**מה לחפש:**
- ✅ `Processing pending notifications...`
- ✅ `Sent X/Y notifications`
- ✅ `User {userId} filtered out` (אם יש משתמשים שכיבו התראות)
- ❌ שגיאות או warnings

---

## 🧪 בדיקה הפוכה: כיבוי התראות

### שלב 1: כבה התראות חדשות

**באפליקציה:**
1. לך למסך **פרופיל** > **התראות**
2. **כבה** את "**התראות חדשות**" ❌

**או במסד הנתונים:**
```sql
-- כבה התראות חדשות למשתמש ספציפי
UPDATE user_notification_settings
SET news_notifications = false
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'YOUR_EMAIL@example.com');
```

---

### שלב 2: הוסף חדשה חדשה שוב

```sql
-- הוסף חדשה חדשה נוספת לבדיקה
INSERT INTO app_news (
  title,
  content,
  source,
  label,
  text_content
)
VALUES (
  '🚫 בדיקה - התראות כבויות - ' || TO_CHAR(NOW(), 'HH24:MI:SS'),
  'זו בדיקה כשההתראות כבויות. אתה לא אמור לקבל התראה!',
  'מערכת בדיקה',
  'בדיקה - כבוי',
  'בדיקה כשההתראות כבויות'
);
```

---

### שלב 3: בדוק שלא נוצרה התראה

```sql
-- בדוק שההתראה לא נוצרה למשתמש שכיבה התראות
SELECT 
  pn.id,
  pn.user_id,
  pn.title,
  u.email,
  uns.news_notifications
FROM pending_notifications pn
LEFT JOIN auth.users u ON pn.user_id = u.id
LEFT JOIN user_notification_settings uns ON pn.user_id = uns.user_id
WHERE pn.notification_type = 'news'
  AND pn.created_at > NOW() - INTERVAL '1 minute'
  AND u.email = 'YOUR_EMAIL@example.com';
```

**צריך להיות:**
- ❌ אין התראה למשתמש שכיבה התראות חדשות!
- ✅ יש התראות למשתמשים אחרים שרוצים התראות חדשות

---

### שלב 4: בדיקה באפליקציה

1. **סגור את האפליקציה לחלוטין**
2. **המתן 30 שניות**
3. **אמור לא לקבל התראה** 📵

---

### שלב 5: הפעל שוב התראות

**באפליקציה:**
1. לך למסך **פרופיל** > **התראות**
2. **הפעל** את "**התראות חדשות**" ✅

**או במסד הנתונים:**
```sql
-- הפעל שוב התראות חדשות
UPDATE user_notification_settings
SET news_notifications = true
WHERE user_id = (SELECT id FROM auth.users WHERE email = 'YOUR_EMAIL@example.com');
```

---

## 📊 שאילתות שימושיות לבדיקות

### בדיקת סטטיסטיקה כללית

```sql
-- כמה משתמשים יש עם device tokens פעילים?
SELECT 
  COUNT(DISTINCT user_id) as users_with_tokens,
  COUNT(*) as total_tokens,
  COUNT(*) FILTER (WHERE is_active = true) as active_tokens
FROM device_tokens;

-- כמה משתמשים רוצים התראות חדשות?
SELECT 
  COUNT(*) FILTER (WHERE news_notifications = true) as wants_news,
  COUNT(*) FILTER (WHERE news_notifications = false) as doesnt_want_news,
  COUNT(*) as total_users
FROM user_notification_settings;
```

### בדיקת התראות אחרונות

```sql
-- התראות אחרונות (10 האחרונות)
SELECT 
  pn.id,
  pn.user_id,
  pn.title,
  pn.notification_type,
  pn.is_sent,
  pn.created_at,
  pn.sent_at,
  EXTRACT(EPOCH FROM (pn.sent_at - pn.created_at)) as seconds_to_send
FROM pending_notifications pn
WHERE pn.created_at > NOW() - INTERVAL '1 hour'
ORDER BY pn.created_at DESC
LIMIT 10;
```

---

## ❌ פתרון בעיות

### אין device token

**פתרון:**
1. פתח את האפליקציה והתחבר
2. חכה 5 שניות
3. בדוק שוב ב-SQL

### אין התראות ב-pending_notifications

**בדוק:**
1. האם הטריגר פעיל?
   ```sql
   SELECT * FROM pg_trigger WHERE tgname = 'on_new_news_article';
   ```
2. האם יש device tokens פעילים?
   ```sql
   SELECT COUNT(*) FROM device_tokens WHERE is_active = true;
   ```
3. בדוק את ה-Logs של PostgreSQL

### התראות נוצרות אבל לא נשלחות

**בדוק:**
1. Edge Function Logs ב-Supabase Dashboard
2. האם pg_net extension פעיל?
   ```sql
   SELECT * FROM pg_extension WHERE extname = 'pg_net';
   ```
3. האם Service Role Key נכון?

### לא מקבל התראה באפליקציה

**בדוק:**
1. האם האפליקציה סגורה לחלוטין? (לא ברקע)
2. האם המשתמש נתן הרשאות להתראות במכשיר?
3. האם ההתראה נשלחה (ב-Logs)?
4. האם ה-device token תקין?

---

## ✅ רשימת בדיקה

- [ ] יש לי device token פעיל
- [ ] התראות חדשות מופעלות במסך ההגדרות
- [ ] הוספתי חדשה חדשה לבדיקה
- [ ] ההתראה נוצרה ב-pending_notifications
- [ ] ההתראה נשלחה (is_sent = true)
- [ ] קיבלתי התראה באפליקציה
- [ ] לחצתי על ההתראה והאפליקציה נפתחה
- [ ] בדיקה הפוכה - כיבוי התראות עובד

---

**בהצלחה בבדיקות!** 🎉



