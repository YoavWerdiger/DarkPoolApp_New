# 📱 בדיקת התראה במכשיר - למה לא הגיעה?

## ✅ מה עובד:
- ✅ ההתראה נוצרה ב-`pending_notifications`
- ✅ ה-Edge Function קראה את ההתראה
- ✅ ה-Edge Function החזירה `sent: 1`
- ❌ אבל ההתראה לא הגיעה למכשיר

---

## 🔍 סיבות אפשריות:

### 1. האפליקציה פתוחה ברקע/פעילה
**הבעיה:** Expo Push Notifications לא תמיד מציגות התראות כשהאפליקציה פעילה.

**פתרון:**
- סגור את האפליקציה לחלוטין
- או תן לה להיות ברקע (לא פעילה)
- הוסף חדשה חדשה שוב

---

### 2. המשתמש לא נתן הרשאות להתראות
**הבעיה:** אם המשתמש לא נתן הרשאות, ההתראות לא יגיעו.

**פתרון:**
- **iOS:** Settings > DarkPool > Notifications > Allow Notifications
- **Android:** Settings > Apps > DarkPool > Notifications > Allow

---

### 3. Device Token לא תקין
**הבעיה:** ה-device token יכול להיות לא תקין או פג תוקף.

**פתרון:**
- התחבר מחדש לאפליקציה
- זה ירשום device token חדש

---

### 4. רץ על סימולטור/אמולטור
**הבעיה:** Expo Push Notifications לא עובד על סימולטור.

**פתרון:**
- **חובה:** הרץ על device פיזי

---

### 5. בעיה ב-Expo Push API
**הבעיה:** ה-Expo Push API החזיר שגיאה.

**פתרון:**
- בדוק את הלוגים של Edge Function
- לך ל: Supabase Dashboard > Edge Functions > `process-pending-notifications` > Logs
- חפש שגיאות או warnings

---

## 🔍 בדיקות:

### בדיקה 1: בדוק את הלוגים של Edge Function
1. לך ל: Supabase Dashboard > Edge Functions > `process-pending-notifications` > Logs
2. חפש את הקריאה האחרונה
3. בדוק אם יש שגיאות או warnings

**מה לחפש:**
- `✅ Sent X/Y notifications for user [user-id]`
- `❌ Failed to send notifications`
- `Expo Push API error`

---

### בדיקה 2: בדוק את ה-device token
```sql
SELECT 
  dt.id,
  dt.user_id,
  LEFT(dt.expo_push_token, 30) || '...' as token_preview,
  dt.platform,
  dt.is_active,
  dt.created_at,
  u.email
FROM device_tokens dt
LEFT JOIN auth.users u ON dt.user_id = u.id
WHERE dt.user_id = 'af781bb1-0529-4d80-9424-6564ec29457e'
  AND dt.is_active = true;
```

**מה לחפש:**
- האם ה-token נראה תקין? (אמור להתחיל ב-`ExponentPushToken[`)
- האם `is_active = true`?
- האם `platform` נכון? (ios/android)

---

### בדיקה 3: נסה לשלוח התראה ידנית דרך Expo
אפשר לבדוק אם ה-token תקין על ידי שליחה ידנית דרך Expo Push Tool:
https://expo.dev/notifications

---

### בדיקה 4: בדוק את ההגדרות באפליקציה
1. פתח את האפליקציה
2. לך למסך הגדרות התראות
3. ודא שההתראות מופעלות
4. ודא ש-"התראות חדשות" מופעלות

---

## 🚀 פתרון מהיר:

### שלב 1: בדוק את הלוגים
לך ל-Edge Functions > `process-pending-notifications` > Logs ובדוק אם יש שגיאות.

### שלב 2: ודא שהמכשיר מוכן
- סגור את האפליקציה לחלוטין
- ודא שהמכשיר פיזי (לא סימולטור)
- ודא שההרשאות מופעלות

### שלב 3: נסה שוב
- הוסף חדשה חדשה
- קרא ל-Edge Function
- בדוק אם ההתראה הגיעה

---

## 📝 הערות:

1. **אם ה-Edge Function החזירה `sent: 1`** - זה אומר שהיא ניסתה לשלוח
2. **אם ההתראה לא הגיעה** - יכול להיות בעיה ב-Expo Push API או במכשיר
3. **בדוק את הלוגים** - זה יראה מה קרה בפועל

---

## ✅ סיכום:

הכל עובד בצד השרת! הבעיה היא שההתראה לא מגיעה למכשיר. בדוק:
1. הלוגים של Edge Function
2. האם האפליקציה סגורה
3. האם המשתמש נתן הרשאות
4. האם זה device פיזי


