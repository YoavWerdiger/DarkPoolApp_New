# 🔧 תיקון אייקון ושם האפליקציה בהתראות

## הבעיה:
ההתראות מציגות את האייקון של Expo Go במקום את האייקון של DarkPool.

## הפתרון:

### 1. עדכון `app.json` ✅
עדכנתי את `app.json` עם ההגדרות הנכונות:
- `notification.icon`: האייקון של ההתראות
- `notification.androidCollapsedTitle`: שם האפליקציה ב-Android
- `plugins`: הגדרות `expo-notifications`

### 2. בניית Build חדש 🔨
**חשוב מאוד:** צריך לבנות build חדש כדי שההגדרות ייכנסו לתוקף!

```bash
eas build --platform android --profile production
```

### 3. התקנה על המכשיר 📱
אחרי שהבילד מוכן:
1. הורד את ה-APK/AAB מהבילד
2. התקן על המכשיר הפיזי
3. בדוק שההתראות מציגות את האייקון והשם של DarkPool

## הערות חשובות:

### ⚠️ Expo Go:
אם אתה רץ על **Expo Go**, זה תמיד יציג את האייקון של Expo Go. זה לא ניתן לשינוי.

### ✅ Production Build:
רק ב-**Production Build** (standalone app) ההתראות יציגו את האייקון והשם של DarkPool.

## מה לבדוק אחרי Build חדש:
1. האם האייקון של DarkPool מופיע בהתראות?
2. האם השם "DarkPool" מופיע במקום "Expo Go"?
3. האם ה-subtitle "DarkPool" מופיע?

## אם עדיין לא עובד:
1. בדוק שהאייקון `./assets/icon.png` קיים
2. בדוק שההגדרות ב-`app.json` נכונות
3. נסה build חדש


