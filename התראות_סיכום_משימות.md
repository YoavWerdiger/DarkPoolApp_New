# סיכום משימות להתראות Push

## ✅ מה שכבר נעשה (בקוד):

### 1. RLS Policies לטבלת pending_notifications
- ✅ נוספו RLS policies ב-`create_news_notification_trigger.sql`
- ✅ משתמשים יכולים לראות רק את ההתראות שלהם
- ✅ Service role יכול לעשות הכל (לצורך Edge Functions)

### 2. Notification Listeners ב-App.tsx
- ✅ נוסף טיפול בהתראות שמגיעות כשהאפליקציה פתוחה
- ✅ נוסף טיפול בלחיצה על התראה
- ✅ ניווט אוטומטי לטאב חדשות כשמשתמש לוחץ על התראה של חדשה

### 3. רישום Device Tokens
- ✅ `NotificationService.registerDeviceToken()` נקרא אוטומטית ב-`AuthContext` כשמשתמש נכנס
- ✅ הטבלה `device_tokens` מוכנה עם RLS policies

### 4. Edge Functions
- ✅ `send-push-notification` - קיים ומוכן
- ✅ `process-pending-notifications` - קיים ומוכן

### 5. Trigger לחדשות
- ✅ Trigger `on_new_news_article` יוצר התראות ב-`pending_notifications` כשנוספת חדשה

---

## ⚠️ מה שצריך לעשות ידנית:

### 1. הרצת SQL Scripts ב-Supabase

היכנס ל-Supabase Dashboard > SQL Editor והרץ את הקבצים הבאים בסדר:

1. **`create_device_tokens_table.sql`**
   - יוצר את טבלת `device_tokens`
   - מגדיר RLS policies

2. **`create_news_notification_trigger.sql`**
   - יוצר את טבלת `pending_notifications`
   - מגדיר RLS policies
   - יוצר את ה-trigger `on_new_news_article`
   - יוצר את הפונקציה `queue_news_notification()`

### 2. העלאה של Edge Functions ל-Supabase

השתמש ב-Supabase CLI להעלות את ה-Edge Functions:

```bash
# ודא שאתה בתיקיית הפרויקט
cd /Users/yoavwerdiger/DarkPoolApp_New-1

# התחבר ל-Supabase (אם עדיין לא)
supabase login

# קשר את הפרויקט (אם עדיין לא)
supabase link --project-ref YOUR_PROJECT_REF

# העלה את ה-Edge Functions
supabase functions deploy send-push-notification
supabase functions deploy process-pending-notifications
```

**או דרך Supabase Dashboard:**
1. היכנס ל-Supabase Dashboard
2. עבור ל-Edge Functions
3. העלה את הקבצים מתיקיית `supabase/functions/`

### 3. הגדרת Cron Job (אופציונלי - מומלץ)

אם תרצה לשלוח התראות דרך cron job במקום trigger ישיר:

1. היכנס ל-Supabase Dashboard
2. עבור ל-Edge Functions > Cron Jobs
3. הוסף cron job חדש:
   - **Function**: `process-pending-notifications`
   - **Schedule**: `*/5 * * * *` (כל 5 דקות)
   - **Method**: POST

**הערה:** אם לא תגדיר cron job, ההתראות יישמרו ב-`pending_notifications` אבל לא יישלחו אוטומטית. תצטרך לקרוא ל-`process-pending-notifications` ידנית או דרך trigger אחר.

---

## 🧪 בדיקות

### בדיקה 1: רישום Device Token
1. התחבר לאפליקציה
2. בדוק ב-Supabase Dashboard > Table Editor > `device_tokens`
3. אמור לראות רשומה עם ה-token שלך

### בדיקה 2: יצירת התראה
1. הוסף חדשה חדשה ל-`app_news` (דרך Supabase או דרך האפליקציה)
2. בדוק ב-`pending_notifications` - אמור לראות התראות לכל המשתמשים עם device tokens פעילים

### בדיקה 3: שליחת התראה
1. קרא ל-Edge Function `process-pending-notifications` (דרך Supabase Dashboard או CLI)
2. בדוק שההתראות נשלחו (בדוק ב-logs של Edge Function)
3. בדוק שההתראות מסומנות כ-`is_sent = true`

### בדיקה 4: קבלת התראה
1. סגור את האפליקציה (או תן לה להיות ברקע)
2. הוסף חדשה חדשה
3. אמור לקבל התראה push

### בדיקה 5: לחיצה על התראה
1. לחץ על התראה
2. האפליקציה אמורה להיפתח ולנווט לטאב חדשות

---

## 📝 הערות חשובות

1. **Expo Project ID**: כבר מוגדר ב-`app.json` כ-`c6140546-bf96-4ca0-85a5-26807f0742f6`

2. **הרשאות**: האפליקציה מבקשת הרשאות אוטומטית כשמשתמש נכנס

3. **הגדרות משתמש**: המערכת בודקת את ההגדרות מ-`AsyncStorage`:
   - `newsNotifications`: האם להפעיל התראות חדשות
   - `notifications`: האם להפעיל התראות כלליות

4. **Production**: ודא שה-Project ID נכון ב-production

---

## 🔧 פתרון בעיות

### אין התראות:
- ✅ בדוק שה-device token נרשם ב-`device_tokens`
- ✅ בדוק שההתראות נוצרות ב-`pending_notifications`
- ✅ בדוק את ה-logs של Edge Functions

### התראות לא נשלחות:
- ✅ בדוק שה-Edge Functions הועלו ל-Supabase
- ✅ בדוק את ה-logs של Edge Functions
- ✅ בדוק שה-Expo Push Token תקין

### הרשאות:
- ✅ ודא שהמשתמש נתן הרשאות להתראות
- ✅ בדוק ב-`device_tokens` שה-`is_active = true`

---

## 📚 קבצים רלוונטיים

- `services/notificationService.ts` - שירות להתראות
- `context/AuthContext.tsx` - רישום device token
- `App.tsx` - טיפול בהתראות וניווט
- `create_device_tokens_table.sql` - יצירת טבלת device tokens
- `create_news_notification_trigger.sql` - יצירת trigger והתראות
- `supabase/functions/send-push-notification/index.ts` - Edge Function לשליחת התראות
- `supabase/functions/process-pending-notifications/index.ts` - Edge Function לעיבוד התראות ממתינות







