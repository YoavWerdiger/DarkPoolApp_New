# ✅ בדיקות אימות - התראות Push לאחר הרצת SQL

## 🎉 מה הושלם

1. ✅ **טבלת הגדרות נוצרה:** `user_notification_settings`
2. ✅ **טריגר התראות חדשות עודכן:** עם בדיקת הגדרות

---

## 🧪 בדיקות אימות - מה לבדוק עכשיו

### בדיקה 1: וידוא שהטבלה נוצרה

```sql
-- בדוק שהטבלה קיימת ומבנה תקין
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_name = 'user_notification_settings'
ORDER BY ordinal_position;
```

**מה אמור להיראות:**
- `user_id` (uuid, NOT NULL, PK)
- `notifications_enabled` (boolean, NOT NULL, default true)
- `news_notifications` (boolean, NOT NULL, default true)
- `earnings_notifications` (boolean, NOT NULL, default true)
- וכו'...

---

### בדיקה 2: וידוא שיש הגדרות למשתמשים קיימים

```sql
-- בדוק כמה משתמשים יש עם הגדרות
SELECT COUNT(*) as users_with_settings
FROM user_notification_settings;
```

**אמור להיות:** מספר המשתמשים הקיימים במערכת (או קרוב לזה)

---

### בדיקה 3: בדיקת טריגר התראות חדשות

```sql
-- בדוק שהטריגר קיים ופעיל
SELECT 
  tgname as trigger_name,
  tgrelid::regclass as table_name,
  tgenabled as enabled,
  pg_get_triggerdef(oid) as definition
FROM pg_trigger
WHERE tgname = 'on_new_news_article';
```

**אמור להראות:**
- `trigger_name`: `on_new_news_article`
- `table_name`: `app_news`
- `enabled`: `O` (O = enabled)

---

### בדיקה 4: בדיקת פונקציה מעודכנת

```sql
-- בדוק שהפונקציה קיימת ומכילה את הלוגיקה החדשה
SELECT 
  routine_name,
  routine_definition
FROM information_schema.routines
WHERE routine_name = 'send_news_notification_immediately';
```

**או בצורה יותר קריאה:**

```sql
-- הצגת הפונקציה המלאה
\df+ send_news_notification_immediately
```

---

### בדיקה 5: בדיקת device tokens פעילים

```sql
-- בדוק כמה משתמשים יש עם device tokens פעילים
SELECT 
  COUNT(DISTINCT user_id) as users_with_tokens,
  COUNT(*) as total_tokens,
  COUNT(*) FILTER (WHERE is_active = true) as active_tokens
FROM device_tokens;
```

**מטרה:** לוודא שיש משתמשים עם device tokens כדי לבדוק התראות

---

### בדיקה 6: בדיקה ידנית - כיבוי והפעלה של התראות

**שלב 1: בדוק משתמש ספציפי**
```sql
-- בדוק את ההגדרות של משתמש מסוים
SELECT * 
FROM user_notification_settings 
WHERE user_id = 'YOUR_USER_ID_HERE';
```

**שלב 2: כבה התראות חדשות למשתמש הזה**
```sql
-- כבה התראות חדשות למשתמש
UPDATE user_notification_settings
SET news_notifications = false
WHERE user_id = 'YOUR_USER_ID_HERE';
```

**שלב 3: הוסף חדשה חדשה**
```sql
-- הוסף חדשה חדשה (לבדיקה)
INSERT INTO app_news (title, content, source, label, text_content)
VALUES (
  'בדיקת התראות',
  'זה בדיקה',
  'מערכת',
  'חדשה לבדיקה',
  'חדשה לבדיקת מערכת התראות'
);
```

**שלב 4: בדוק שההתראה לא נוצרה למשתמש הזה**
```sql
-- בדוק התראות ממתינות
SELECT * 
FROM pending_notifications 
WHERE notification_type = 'news'
  AND article_id = (SELECT id FROM app_news WHERE title = 'בדיקת התראות' ORDER BY created_at DESC LIMIT 1)
ORDER BY created_at DESC;
```

**צריך להיות:**
- אין התראה למשתמש שכיבה התראות חדשות
- יש התראות למשתמשים אחרים שרוצים התראות חדשות

**שלב 5: הפעל שוב התראות חדשות**
```sql
-- הפעל שוב התראות חדשות
UPDATE user_notification_settings
SET news_notifications = true
WHERE user_id = 'YOUR_USER_ID_HERE';
```

---

### בדיקה 7: בדיקת Edge Function

**בדוק ב-Logs של Supabase:**

1. היכנס ל-Supabase Dashboard
2. עבור ל-Edge Functions > `process-pending-notifications` > Logs
3. הוסף חדשה חדשה ל-`app_news`
4. אמור לראות קריאה ל-Edge Function

**מה לחפש:**
- ✅ `Processing pending notifications...`
- ✅ `Sent X/Y notifications`
- ❌ שגיאות או warnings

---

### בדיקה 8: בדיקת התראות בפועל (באפליקציה)

1. **פתח את האפליקציה** והתחבר
2. **לך למסך הגדרות התראות**
3. **כבה התראות חדשות**
4. **סגור את האפליקציה** (או תן לה להיות ברקע)
5. **הוסף חדשה חדשה** ל-`app_news` (דרך Supabase Dashboard)
6. **אמור לא לקבל התראה** 📵

**בדיקה הפוכה:**
1. **הפעל התראות חדשות** במסך ההגדרות
2. **סגור את האפליקציה**
3. **הוסף חדשה חדשה** ל-`app_news`
4. **אמור לקבל התראה** 📱

---

## 🔍 שאילתות שימושיות לניפוי באגים

### בדיקת כל ההתראות האחרונות
```sql
SELECT 
  id,
  user_id,
  title,
  notification_type,
  is_sent,
  created_at,
  sent_at
FROM pending_notifications
ORDER BY created_at DESC
LIMIT 20;
```

### בדיקת משתמשים עם התראות שכובות
```sql
SELECT 
  uns.user_id,
  uns.news_notifications,
  uns.earnings_notifications,
  uns.economic_calendar_notifications,
  uns.message_notifications,
  COUNT(dt.id) as device_tokens_count
FROM user_notification_settings uns
LEFT JOIN device_tokens dt ON uns.user_id = dt.user_id AND dt.is_active = true
WHERE uns.news_notifications = false
   OR uns.earnings_notifications = false
   OR uns.economic_calendar_notifications = false
   OR uns.message_notifications = false
GROUP BY uns.user_id, uns.news_notifications, uns.earnings_notifications, 
         uns.economic_calendar_notifications, uns.message_notifications;
```

### בדיקת התראות שלא נשלחו
```sql
SELECT 
  notification_type,
  COUNT(*) as pending_count,
  MIN(created_at) as oldest_pending,
  MAX(created_at) as newest_pending
FROM pending_notifications
WHERE is_sent = false
GROUP BY notification_type;
```

---

## ✅ רשימת סיכום - מה צריך לעבוד

- [ ] טבלת `user_notification_settings` קיימת
- [ ] יש הגדרות ברירת מחדל למשתמשים קיימים
- [ ] טריגר `on_new_news_article` קיים ופעיל
- [ ] פונקציה `send_news_notification_immediately` מעודכנת
- [ ] כשכבים התראות חדשות → לא נוצרות התראות
- [ ] כשהתראות חדשות פעילות → נוצרות התראות
- [ ] Edge Function `process-pending-notifications` מקבל התראות
- [ ] ההתראות נשלחות למשתמשים (בדיקה בפועל באפליקציה)

---

## 🐛 פתרון בעיות נפוצות

### בעיה: אין התראות בכלל

**פתרון:**
1. בדוק שיש device tokens פעילים
2. בדוק שההתראות נוצרות ב-`pending_notifications`
3. בדוק את ה-Logs של Edge Functions

---

### בעיה: התראות נשלחות גם למשתמשים שכיבו

**פתרון:**
1. בדוק שהטריגר עודכן (הרץ שוב את `update_news_notification_trigger_with_settings.sql`)
2. בדוק שהפונקציה מכילה את ה-JOIN ל-`user_notification_settings`
3. בדוק שההגדרות נשמרות במסד הנתונים (לא רק AsyncStorage)

---

### בעיה: Edge Function לא נקראת

**פתרון:**
1. בדוק ש-pg_net extension פעיל:
   ```sql
   SELECT * FROM pg_extension WHERE extname = 'pg_net';
   ```
2. בדוק שה-Service Role Key נכון
3. בדוק שה-URL נכון: `https://wpmrtczbfcijoocguime.supabase.co/functions/v1/process-pending-notifications`

---

## 📞 הבא

אחרי שכל הבדיקות עברו בהצלחה:
- ✅ המערכת מוכנה לשימוש!
- ⏳ אפשר להוסיף התראות דיווחי רווחים
- ⏳ אפשר להוסיף התראות יומן כלכלי
- ⏳ אפשר להוסיף התראות הודעות צ'אט

---

**תאריך:** {{ היום }}
**סטטוס:** מוכן לבדיקות ✅



