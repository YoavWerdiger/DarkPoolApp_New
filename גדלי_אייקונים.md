# גדלי אייקונים – DarkPool

## 1. אייקון התראה (Notification – הסמל הקטן ליד ההתראה)

באנדרואיד האייקון **הקטן** מוצג רק בלבן (alpha). אפשר להשתמש ב-**Vector** (כמו `ic_notification.xml` – 24×24 dp) או ב-**PNG**.

### אם אתה יוצר PNG (לבן על רקע שקוף):

| תיקייה            | גודל בפיקסלים |
|-------------------|-----------------|
| drawable-mdpi     | 24 × 24         |
| drawable-hdpi     | 36 × 36         |
| drawable-xhdpi    | 48 × 48         |
| drawable-xxhdpi   | 72 × 72         |
| drawable-xxxhdpi  | 96 × 96         |

**חשוב:** רק לבן (#FFFFFF) על רקע שקוף. בלי צבעים.

### Vector (מה שיש עכשיו)
- `ic_notification.xml` – 24×24 **dp** (המערכת scaled אוטומטית). מספיק קובץ אחד.

---

## 2. אייקון אפליקציה (App Icon / Launcher)

### קובץ ראשי (app.json → `icon`, `adaptiveIcon.foregroundImage`)
- **1024 × 1024 px** – גודל מומלץ אחד, Expo/Android ייצרו מזה את כל הצפיפויות.
- פורמט: PNG, רקע שקוף או לא (לפי העיצוב).

### Adaptive Icon (אנדרואיד)
- **1024 × 1024 px** – אותו קובץ ל־`foregroundImage` (ול־`monochromeImage` אם יש).
- **אזור בטוח:** הלוגו החשוב עדיף שיהיה בתוך **מעגל בקוטר ~66%** מהמרכז (כי המערכת חותכת לעיגול/צורות).

---

## 3. Splash (מסך הפתיחה)
- **1284 × 2778 px** (או יחס דומה) – מספיק קובץ אחד גדול, Expo scaled.
- או גודל מינימלי סביר (למשל 1242 × 2688) כדי לא להכביד.

---

## 4. תמונה גדולה בהתראה (Big Picture / richContent)

- זו **תמונה בתוך גוף ההתראה**, לא אייקון.
- מומלץ: **רוחב עד ~600 px**, יחס 1:1 או 16:9, קובץ לא כבד (למשל עד 300KB).
- חייבת להיות **URL ציבורי** (HTTPS) כדי שהמערכת תוכל לטעון.

---

## סיכום מהיר

| שימוש              | גודל מומלץ        | הערות                    |
|--------------------|--------------------|---------------------------|
| אייקון התראה (PNG)| 24–96 px לפי dpi   | לבן על שקוף בלבד         |
| אייקון התראה (Vector) | 24×24 dp       | קובץ אחד (כמו עכשיו)     |
| אייקון אפליקציה   | **1024 × 1024 px** | PNG, אזור בטוח במרכז     |
| Splash             | 1284 × 2778 (או דומה) | PNG/JPEG              |
| תמונה בהתראה       | עד ~600 px רוחב    | URL ציבורי, לא כבד        |
