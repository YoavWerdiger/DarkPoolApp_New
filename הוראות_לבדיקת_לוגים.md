# 🔍 הוראות לבדיקת לוגים - למה הטוקן לא נשמר?

## ✅ מה כבר בדקנו:
- ✅ מבנה הטבלה תקין
- ✅ RLS מופעל
- ✅ כל ה-Policies קיימים (SELECT, INSERT, UPDATE, DELETE)

## 🔍 מה צריך לבדוק עכשיו:

### אפשרות 1: אם יש לך גישה ל-`adb` (המכשיר/אמולטור מחובר למחשב)

```bash
adb logcat | grep -E "NotificationService|AuthContext"
```

זה יראה את כל הלוגים ויעזור להבין:
- האם `getExpoPushToken` מחזיר טוקן או `null`?
- האם יש שגיאה ב-INSERT?
- מה השגיאה המדויקת?

### אפשרות 2: אם אין לך `adb`

1. **פתח את האפליקציה**
2. **התחבר למשתמש**
3. **חכה 5-10 שניות** - האפליקציה אמורה לנסות לרשום את הטוקן
4. **העתק את כל הלוגים** מהקונסול (אם יש לך React Native Debugger או כלי אחר)

### אפשרות 3: בדיקה במסד הנתונים

אחרי התחברות, הרץ:

```sql
-- בדוק אם יש טוקנים חדשים
SELECT 
  dt.id,
  dt.user_id,
  LEFT(dt.expo_push_token, 30) || '...' as token_preview,
  dt.platform,
  dt.device_id,
  dt.is_active,
  dt.created_at,
  u.email
FROM device_tokens dt
LEFT JOIN auth.users u ON dt.user_id = u.id
WHERE dt.created_at > NOW() - INTERVAL '5 minutes'
ORDER BY dt.created_at DESC;
```

## 🔍 מה לחפש בלוגים:

### אם `getExpoPushToken` מחזיר `null`:
```
❌ NotificationService: No token received!
⚠️ NotificationService: Device.isDevice = false
```

**פתרון אפשרי:**
- זה אמולטור/סימולטור - צריך מכשיר פיזי
- או שיש בעיה עם Firebase initialization

### אם יש שגיאה ב-INSERT:
```
❌ NotificationService: Error inserting device token
❌ NotificationService: Error code: [קוד שגיאה]
```

**פתרון:**
- אם קוד שגיאה `42501` - בעיה ב-RLS (אבל כבר בדקנו שזה תקין)
- אם קוד שגיאה `23505` - duplicate key (זה בסדר, הקוד מטפל בזה)
- אם קוד שגיאה אחר - צריך לבדוק מה זה

### אם הכל נראה תקין אבל הטוקן לא נשמר:
- אולי יש בעיה עם `auth.uid()` - המשתמש לא מזוהה נכון
- אולי יש בעיה עם הרשת

## 📝 הערות:
- הקוד מדפיס לוגים מפורטים מאוד
- כל שלב מדפיס לוגים
- אם יש שגיאה, היא תופיע עם כל הפרטים


