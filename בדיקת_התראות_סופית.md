# בדיקת התראות Push - סופית

## ✅ מה שכבר נעשה:

1. ✅ SQL Scripts הורצו (`create_device_tokens_table.sql`, `create_news_notification_trigger.sql`)
2. ✅ Edge Functions הועלו (`send-push-notification`, `process-pending-notifications`)
3. ✅ pg_net extension הופעל
4. ✅ Trigger מוגדר עם Service Role Key
5. ✅ Notification listeners ב-App.tsx
6. ✅ רישום device tokens אוטומטי

---

## 🧪 בדיקות:

### בדיקה 1: רישום Device Token

1. **פתח את האפליקציה** והתחבר
2. **בדוק ב-Supabase Dashboard > Table Editor > `device_tokens`**
   - אמור לראות רשומה עם ה-token שלך
   - `is_active` צריך להיות `true`
   - `user_id` צריך להיות ה-ID שלך

**אם אין רשומה:**
- בדוק ב-Logs של האפליקציה אם יש שגיאות
- ודא שהמשתמש נתן הרשאות להתראות

---

### בדיקה 2: יצירת התראה

1. **הוסף חדשה חדשה** ל-`app_news`:
   - דרך Supabase Dashboard > Table Editor > `app_news`
   - או דרך האפליקציה (אם יש לך דרך להוסיף)

2. **בדוק ב-`pending_notifications`**:
   - אמור לראות התראות חדשות לכל המשתמשים עם device tokens פעילים
   - `is_sent` צריך להיות `false` (עד שההתראות נשלחו)

---

### בדיקה 3: שליחת התראה (החשוב ביותר!)

1. **בדוק ב-Logs של Edge Functions**:
   - Supabase Dashboard > Edge Functions > `process-pending-notifications` > Logs
   - אמור לראות קריאה ל-Edge Function
   - אמור לראות `✅ Sent X/Y notifications`

2. **אם אין קריאה ל-Edge Function:**
   - בדוק שה-trigger עובד:
     ```sql
     -- בדוק שה-trigger קיים
     SELECT * FROM pg_trigger WHERE tgname = 'on_new_news_article';
     ```
   - בדוק שה-pg_net extension פעיל:
     ```sql
     SELECT * FROM pg_extension WHERE extname = 'pg_net';
     ```
   - בדוק שה-Service Role Key נכון

3. **אם יש שגיאה ב-Logs:**
   - בדוק שה-Service Role Key נכון
   - בדוק שה-URL נכון: `https://wpmrtczbfcijoocguime.supabase.co/functions/v1/process-pending-notifications`

---

### בדיקה 4: קבלת התראה

1. **סגור את האפליקציה** (או תן לה להיות ברקע)
2. **הוסף חדשה חדשה** ל-`app_news`
3. **אמור לקבל התראה push** תוך כמה שניות

**אם לא קיבלת התראה:**
- בדוק שהמכשיר מחובר לאינטרנט
- בדוק שהמשתמש נתן הרשאות להתראות
- בדוק ב-Logs של Edge Functions אם יש שגיאות
- בדוק שה-device token תקין ב-`device_tokens`

---

### בדיקה 5: לחיצה על התראה

1. **לחץ על התראה** שקיבלת
2. **האפליקציה אמורה להיפתח** ולנווט לטאב חדשות

**אם לא נווט:**
- בדוק ב-Logs של האפליקציה אם יש שגיאות
- ודא שה-navigation listeners מוגדרים ב-`App.tsx`

---

## 🔧 פתרון בעיות נפוצות:

### אין התראות בכלל:
- ✅ בדוק שה-device token נרשם ב-`device_tokens`
- ✅ בדוק שההתראות נוצרות ב-`pending_notifications`
- ✅ בדוק את ה-logs של Edge Functions
- ✅ בדוק שה-trigger עובד (הוסף חדשה ובדוק אם נוצרת התראה)

### התראות נוצרות אבל לא נשלחות:
- ✅ בדוק ב-Logs של Edge Functions אם יש קריאה ל-`process-pending-notifications`
- ✅ אם אין קריאה, בדוק שה-pg_net extension פעיל
- ✅ בדוק שה-Service Role Key נכון
- ✅ בדוק שה-URL נכון

### התראות נשלחות אבל לא מגיעות:
- ✅ בדוק שהמשתמש נתן הרשאות להתראות
- ✅ בדוק שה-device token תקין
- ✅ בדוק שהמכשיר מחובר לאינטרנט
- ✅ בדוק ב-Logs של Expo Push API (אם יש גישה)

---

## 📝 SQL Queries לבדיקה:

### בדוק שה-trigger קיים:
```sql
SELECT * FROM pg_trigger WHERE tgname = 'on_new_news_article';
```

### בדוק שה-pg_net extension פעיל:
```sql
SELECT * FROM pg_extension WHERE extname = 'pg_net';
```

### בדוק device tokens:
```sql
SELECT * FROM device_tokens WHERE is_active = true;
```

### בדוק התראות ממתינות:
```sql
SELECT * FROM pending_notifications WHERE is_sent = false ORDER BY created_at DESC LIMIT 10;
```

### בדוק התראות שנשלחו:
```sql
SELECT * FROM pending_notifications WHERE is_sent = true ORDER BY sent_at DESC LIMIT 10;
```

---

## 🎉 אם הכל עובד:

**מזל טוב!** 🎊

עכשיו כל פעם שנוספת חדשה חדשה, ההתראות נשלחות **מיידית** - בדיוק כמו וואטסאפ וטלגרם!

**המערכת מוכנה גם להתראות על הודעות צ'אט בעתיד** - פשוט תיצור trigger דומה על טבלת `messages`.

---

## 📚 קבצים רלוונטיים:

- `create_device_tokens_table.sql` - יצירת טבלת device tokens
- `create_news_notification_trigger.sql` - יצירת trigger והתראות
- `supabase/functions/process-pending-notifications/index.ts` - Edge Function לעיבוד התראות
- `supabase/functions/send-push-notification/index.ts` - Edge Function לשליחת התראות
- `services/notificationService.ts` - שירות להתראות
- `App.tsx` - טיפול בהתראות וניווט







