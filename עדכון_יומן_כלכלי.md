# 🚀 מדריך עדכון יומן כלכלי - תאריכים ושעות

## ✅ מה כבר עודכן בקוד:

1. **`daily-economic-sync-simple/index.ts`** - עודכן עם:
   - חילוץ נכון של תאריך ושעה מ-EODHD API
   - המרת timezone מ-UTC/EST לשעון ישראל
   - תיקון פריסה לימים (אירועים ב-00:00-06:00 עוברים ליום הקודם)

2. **`sync-economic-calendar/index.ts`** - עודכן עם:
   - חילוץ נכון של תאריך ושעה מ-EODHD API
   - המרת timezone מ-UTC/EST לשעון ישראל
   - תיקון פריסה לימים
   - תרגום כותרות לעברית

3. **`screens/News/EconomicCalendarTab.tsx`** - עודכן עם:
   - גלילה אוטומטית לאירוע הקרוב ביותר

---

## 📋 מה צריך לעדכן:

### שלב 1: פריסת Edge Functions ל-Supabase

#### א. `daily-economic-sync-simple`

1. לך ל-**Supabase Dashboard** → **Edge Functions**
2. בחר **`daily-economic-sync-simple`**
3. לחץ על **"Deploy"** או העתק את הקוד החדש מ:
   ```
   supabase/functions/daily-economic-sync-simple/index.ts
   ```
4. הדבק ב-Supabase Editor
5. לחץ **"Deploy"**

#### ב. `sync-economic-calendar` (מומלץ יותר - כולל תרגום לעברית)

1. לך ל-**Supabase Dashboard** → **Edge Functions**
2. בחר **`sync-economic-calendar`** (אם לא קיים, צור חדש)
3. העתק את הקוד מ:
   ```
   supabase/functions/sync-economic-calendar/index.ts
   ```
4. הדבק ב-Supabase Editor
5. לחץ **"Deploy"**

---

### שלב 2: עדכון Cron Job

**החלטה:** איזו פונקציה להשתמש?

- **`daily-economic-sync-simple`** - פשוטה, רק EODHD
- **`sync-economic-calendar`** - כוללת תרגום לעברית (מומלץ!)

#### אם בחרת `sync-economic-calendar`:

```sql
-- מחיקת Cron ישן
SELECT cron.unschedule('daily-economic-sync');
SELECT cron.unschedule('daily-economic-sync-simple');

-- יצירת Cron חדש עם Function המשופרת
SELECT cron.schedule(
  'sync-economic-calendar',
  '0 6 * * *',  -- כל יום ב-06:00
  $$
  SELECT net.http_post(
    url := 'https://wpmrtczbfcijoocguime.supabase.co/functions/v1/sync-economic-calendar',
    headers := '{"Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndwbXJ0Y3piZmNpam9vY2d1aW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDczNTEsImV4cCI6MjA2Njc4MzM1MX0.YHfniy3w94LVODC54xb7Us-Daw_pRx2WWFOoR-59kGQ", "Content-Type": "application/json"}'::jsonb,
    body := '{}'::jsonb
  );
  $$
);

-- בדיקה
SELECT jobname, schedule, active FROM cron.job;
```

#### אם בחרת `daily-economic-sync-simple`:

```sql
-- מחיקת Cron ישן
SELECT cron.unschedule('daily-economic-sync');

-- יצירת Cron חדש
SELECT cron.schedule(
  'daily-economic-sync-simple',
  '0 6 * * *',  -- כל יום ב-06:00
  $$
  SELECT net.http_post(
    url := 'https://wpmrtczbfcijoocguime.supabase.co/functions/v1/daily-economic-sync-simple',
    headers := '{"Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndwbXJ0Y3piZmNpam9vY2d1aW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDczNTEsImV4cCI6MjA2Njc4MzM1MX0.YHfniy3w94LVODC54xb7Us-Daw_pRx2WWFOoR-59kGQ", "Content-Type": "application/json"}'::jsonb,
    body := '{}'::jsonb
  );
  $$
);
```

---

### שלב 3: בדיקה ידנית

1. **לך ל-Supabase Dashboard** → **Edge Functions**
2. בחר את הפונקציה שהעדפנת (`sync-economic-calendar` או `daily-economic-sync-simple`)
3. לחץ על **"Invoke"** או **"Test"**
4. בדוק את הלוגים - צריך לראות:
   ```
   📊 Fetched X events
   📅 Event: [שם] - Original: [תאריך] → Parsed: [תאריך מתוקן] [שעה]
   ```

---

### שלב 4: ניקוי נתונים ישנים (אופציונלי)

אם יש נתונים ישנים עם תאריכים/שעות שגויים, אפשר לנקות:

```sql
-- מחיקת כל האירועים הישנים
DELETE FROM economic_events;

-- הפונקציה תמלא מחדש עם הנתונים הנכונים
```

---

## 🔍 בדיקת התוצאות

1. פתח את האפליקציה
2. לך ל-**יומן כלכלי**
3. בדוק:
   - ✅ תאריכים נכונים (אירועים ב-00:00-06:00 מופיעים ביום הקודם)
   - ✅ שעות נכונות (בשעון ישראל)
   - ✅ גלילה אוטומטית לאירוע הקרוב ביותר (כשנמצאים ביום הנוכחי)

---

## 📝 סיכום השינויים

### מה שונה:

1. **חילוץ תאריך ושעה:**
   - לפני: חילוץ פשוט לפי רווח
   - אחרי: פרסור נכון עם טיפול בפורמטים שונים

2. **המרת timezone:**
   - לפני: הוספה פשוטה של 7 שעות
   - אחרי: המרה נכונה מ-UTC/EST לשעון ישראל עם DST

3. **פריסה לימים:**
   - לפני: ללא תיקון
   - אחרי: אירועים ב-00:00-06:00 עוברים ליום הקודם

4. **גלילה אוטומטית:**
   - חדש: הרשימה נגללת אוטומטית לאירוע הקרוב ביותר

---

## 🆘 פתרון בעיות

### הפונקציה לא רצה:
- בדוק שהקוד הועתק נכון
- בדוק את ה-logs ב-Supabase

### תאריכים/שעות עדיין שגויים:
- נקה את הטבלה `economic_events` והרץ מחדש
- בדוק את ה-logs - צריך לראות את הפורמט המקורי מה-API

### אין אירועים:
- בדוק שה-API של EODHD עובד
- בדוק את ה-API key

---

## ✨ המלצה

**השתמש ב-`sync-economic-calendar`** - היא כוללת:
- ✅ כל התיקונים
- ✅ תרגום לעברית
- ✅ לוגים מפורטים יותר


