# 🔄 עדכון תוצאות אירועים כלכליים

## 🎯 מה זה?

פונקציה שמעדכנת **תוצאות (actual values)** לאירועים שכבר התרחשו.

למשל:
- אתמול היה אירוע CPI
- היום יש תוצאה (actual value)
- הפונקציה תעדכן את התוצאה בטבלה

---

## 📋 איך זה עובד?

### 1. **`daily-economic-sync-simple`** (כל יום ב-06:00)
- שולפת אירועים עתידיים וקיימים
- משתמשת ב-`upsert` - כך שאם יש actual value חדש, זה מתעדכן אוטומטית

### 2. **`update-economic-results`** (פונקציה חדשה - אופציונלי)
- מחפשת אירועים ב-7 ימים האחרונים
- מעדכנת תוצאות (actual values) לאירועים שכבר התרחשו
- רצה כל שעה או כל כמה שעות

---

## 🚀 הגדרה מהירה

### אופציה 1: הפונקציה הקיימת מספיקה (מומלץ!)

**`daily-economic-sync-simple` כבר מעדכן תוצאות!**

כי היא משתמשת ב-`upsert`:
- אם האירוע קיים → מתעדכן
- אם יש actual value חדש → הוא נשמר
- כל יום ב-06:00 → הכל מתעדכן

**✅ זה כבר עובד!** לא צריך לעשות כלום.

---

### אופציה 2: עדכון תכוף יותר (למתלהבים)

אם תרצה לעדכן תוצאות **מהר יותר** (כל שעה), תוכל:

#### שלב 1: פרוס את הפונקציה החדשה

1. **לך ל-Supabase Dashboard** → **Edge Functions**
2. **צור פונקציה חדשה:** `update-economic-results`
3. **העתק את הקוד מ:** `supabase/functions/update-economic-results/index.ts`
4. **פרוס**

#### שלב 2: צור Cron Job (כל שעה)

```sql
SELECT cron.schedule(
  'update-economic-results',
  '0 * * * *',  -- כל שעה
  $$
  SELECT net.http_post(
    url := 'https://wpmrtczbfcijoocguime.supabase.co/functions/v1/update-economic-results',
    headers := '{"Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndwbXJ0Y3piZmNpam9vY2d1aW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDczNTEsImV4cCI6MjA2Njc4MzM1MX0.YHfniy3w94LVODC54xb7Us-Daw_pRx2WWFOoR-59kGQ", "Content-Type": "application/json"}'::jsonb,
    body := '{}'::jsonb,
    timeout_milliseconds := 60000
  );
  $$
);
```

---

## 💡 המלצה

**אופציה 1 מספיקה!**

`daily-economic-sync-simple` כבר:
- ✅ מעדכן תוצאות (כי upsert)
- ✅ רצה כל יום ב-06:00
- ✅ מכסה 3 חודשים אחורה + 6 קדימה

**אופציה 2 רק אם:**
- ⚡ רוצה עדכונים כל שעה (במקום פעם ביום)
- ⚡ חשוב מאוד שהתוצאות יעודכנו מיד

---

## 🔍 איך לבדוק שהכל עובד?

### בדיקה ידנית:

1. **לך ל-Edge Functions** → **`daily-economic-sync-simple`**
2. **לחץ "Invoke"**
3. **בדוק את הלוגים** - צריך לראות שהפונקציה רצה

### בדיקה בטבלה:

```sql
-- בדוק אירועים עם תוצאות (actual values)
SELECT title, date, time, actual, forecast, previous
FROM economic_events
WHERE actual IS NOT NULL AND actual != ''
ORDER BY date DESC
LIMIT 10;
```

---

## 📝 סיכום

**כרגע:**
- ✅ `daily-economic-sync-simple` מעדכן הכל כל יום ב-06:00
- ✅ כולל תוצאות (actual values) לאירועים שכבר התרחשו
- ✅ הכל עובד אוטומטית!

**אם תרצה יותר תכוף:**
- 🔄 אפשר להוסיף את `update-economic-results` שירוץ כל שעה

---

## ✨ הכל מוכן!

המערכת כבר מעדכנת תוצאות אוטומטית! 🎉


